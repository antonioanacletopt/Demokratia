---
description: Start the local preview server for the Next.js application
---
# Preview Project Workflow

1. Start the development server.
// turbo
`npm run dev`

2. Access the application in the browser at the URL provided in the terminal (usually http://localhost:3000 or the dynamically assigned port).
